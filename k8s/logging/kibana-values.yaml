# Helm values for Kibana

# Connect to Elasticsearch
elasticsearchHosts: "http://elasticsearch-master:9200"

# Resource limits
resources:
  requests:
    cpu: "200m"
    memory: "512Mi"
  limits:
    cpu: "500m"
    memory: "1Gi"

# Service configuration
service:
  type: ClusterIP
  port: 5601
